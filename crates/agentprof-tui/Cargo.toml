[package]
name        = "agentprof-tui"
publish     = false  # M1.7 D-3 — internal lib; release goes via cli + cargo-dist
description = "Ratatui-based terminal views (flamegraph, ROI table, aggregate) for agentprof."
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
authors.workspace      = true
version.workspace      = true

[lints]
workspace = true

[dependencies]
agentprof-core = { workspace = true }
ratatui        = { workspace = true }
crossterm      = { workspace = true }
chrono         = { workspace = true }
serde_json     = { workspace = true }
thiserror      = { workspace = true }
tracing        = { workspace = true }

[dev-dependencies]
insta = { workspace = true }
agentprof-adapters = { workspace = true }
