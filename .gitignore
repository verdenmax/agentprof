# Rust
target/
**/*.rs.bk
*.pdb

# Cargo
# NOTE: Cargo.lock IS committed because this workspace contains binaries
# (agentprof-cli, xtask). Per Rust convention, binary projects pin
# Cargo.lock for reproducible builds.
.cargo/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs / data
*.log
*.sqlite
*.sqlite-journal
*.db

# agentprof local artifacts
/data/
/tmp/
*.speedscope.json

# insta pending snapshots
*.pending-snap

# Coverage
coverage/
*.profraw

# Local smoke-test target directories (developer-specific; NEVER commit)
/local-fixtures/
/smoke-data/

# superpowers brainstorming visual companion sessions
# (mockup HTML files + state — not source artifacts)
.superpowers/

# Visual guide — generated HTML (source-of-truth is xtask + assets/)
docs/visual-guide/*.html
docs/visual-guide/usage/
docs/visual-guide/wiki/
!docs/visual-guide/.gitkeep
!docs/visual-guide/README.md
