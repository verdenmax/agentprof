# PII Audit Tool + CI Guard (L-11/L-12) Implementation Plan

> Subagent-driven; checkbox steps. Spec: `docs/superpowers/specs/2026-06-29-pii-audit-design.md`.

**Goal:** `cargo xtask audit-pii <path>` flags real home-dir paths; CI `pii-guard` runs it. **Stack:** xtask + walkdir (ws dep), plain string match (no regex). 3 tasks: T1 scanner+wire, T2 CI, T3 docs.

## File Structure
| File | Role |
|---|---|
| `xtask/src/pii.rs` (new) | `AuditPiiCmd{path}` + `run` + scanner + tests |
| `xtask/src/main.rs` | `AuditPii` variant + dispatch + `mod pii` |
| `xtask/Cargo.toml` | `walkdir` dep |
| `.github/workflows/ci.yml` | `pii-guard` job |
| docs | privacy.md §5, ROADMAP, CHANGELOG, xtask README |

---
## Task 1: scanner + wire
**Files:** create `xtask/src/pii.rs`; modify `xtask/src/main.rs`, `xtask/Cargo.toml`.
- [ ] **S1 test** in pii.rs: tmp dir, write `a.txt`="x /home/alice/p", `b.txt`="ok /home/USER/p"; `scan(dir)` → 1 hit on a.txt:1; empty dir → 0. `/Users/bob`+`C:\Users\bob` → hit; binary (`\xff\xfe`) skipped. Run → RED.
- [ ] **S2** add `xtask/Cargo.toml` `[dependencies]` `walkdir = { workspace = true }`.
- [ ] **S3** `pii.rs`: `#[derive(Args)] AuditPiiCmd{ path: PathBuf }`. `pub fn run(c)->anyhow::Result<()>`: `let hits = scan(&c.path)?; for h in &hits { println!("{h}") } if hits.is_empty(){Ok(())} else {std::process::exit(2)}`. `fn scan(root)->Result<Vec<String>>`: walkdir files, skip `target`/`.git`, read_to_string (skip Err=binary), per line check `is_pii(line)` → push `"{path}:{n}: {line.trim()}"`. `fn is_pii(l)`: for pat in ["/home/","/Users/",r"C:\Users\\"]: if `l.contains(pat)` extract next seg, true unless seg∈{"USER","<user>","<username>"}. rustdoc+Examples on pub items.
- [ ] **S4** main.rs: `mod pii;` + `AuditPii(pii::AuditPiiCmd)` + `Cli::AuditPii(c)=>pii::run(c)`.
- [ ] **S5** GREEN; `cargo clippy -p xtask -Dwarnings`; fmt. **S6** commit `feat(xtask): audit-pii path scanner (L-11)`.

## Task 2: CI guard
- [ ] add `.github/workflows/ci.yml` job `pii-guard` (mirror cargo-deny job): checkout + `cargo xtask audit-pii crates`. Run `cargo xtask audit-pii crates` locally → must exit 0 (no real paths committed); if it flags, fix fixtures. commit `ci: pii-guard runs audit-pii over crates (L-12)`.

## Task 3: docs
- [ ] privacy.md §5 L-11/12 → shipped; ROADMAP L-11/L-12 → fixed; CHANGELOG; xtask/README cmd. fmt/doc green. commit `docs: pii-audit L-11/L-12 done`.

## Self-review
covers scanner(T1)+CI(T2)+docs(T3); no regex; allowlist pinned; audit-only.
