# Stable rustfmt options only.
# group_imports / imports_granularity require nightly and are NOT set here.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
